import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  titleText: {
    fontSize: 30,
    fontWeight: "bold",
    marginLeft: 7,
    marginBottom: 5,
  },
});
export default styles;
